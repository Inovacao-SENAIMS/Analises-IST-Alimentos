/* Controla amostras, regras condicionais, validação e envio da solicitação. */
(function () {
  AppAuth.configurarCabecalho();
  const form = document.querySelector('#seed-form'); if (!form) return;
  const sampleList = document.querySelector('#sample-list'); const addButton = document.querySelector('#add-sample'); const count = document.querySelector('#sample-count');
  let opcoes = APP_CONFIG.opcoesPadrao; let amostras = [];
  const ensaios = ['pureza','pms','outrasSementes','infestadas','germinacao','vigorEa','tetrazolio','frio','emergencia'];
  const escapeHtml = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;' }[char]));
  function opcoesHtml(lista, selecionada) { return ['<option value="">Selecione</option>', ...lista.map((item) => `<option value="${escapeHtml(item)}" ${item === selecionada ? 'selected' : ''}>${escapeHtml(item)}</option>`)].join(''); }
  function novoRegistro() { return { especie:'', cultivar:'', safra:'', peneira:'', lote:'', representatividade:'', categoria:'', tratamento:'Não', tratProduto:'', tratPrincipioAtivo:'', tratDosagem:'' }; }
  function renderizarAmostras() {
    sampleList.innerHTML = amostras.map((amostra, indice) => `<article class="sample-card" data-amostra="${indice}"><div class="sample-header"><h3 class="sample-title">Amostra ${indice + 1}</h3><button class="link-button remove-sample" type="button" ${amostras.length === 1 ? 'disabled' : ''}>Remover</button></div><div class="sample-body"><div class="field-grid"><div class="field"><label>Espécie <span>*</span></label><input data-campo="especie" value="${escapeHtml(amostra.especie)}" required><small class="error-message"></small></div><div class="field"><label>Cultivar <span>*</span></label><input data-campo="cultivar" value="${escapeHtml(amostra.cultivar)}" required><small class="error-message"></small></div><div class="field"><label>Safra (ano/ano) <span>*</span></label><input data-campo="safra" placeholder="2023/24" value="${escapeHtml(amostra.safra)}" required><small class="error-message"></small></div><div class="field"><label>Peneira <span>*</span></label><select data-campo="peneira" required>${opcoesHtml(opcoes.peneiras, amostra.peneira)}</select><small class="error-message"></small></div><div class="field"><label>Lote <span>*</span></label><input data-campo="lote" value="${escapeHtml(amostra.lote)}" required><small class="error-message"></small></div><div class="field"><label>Representatividade <span>*</span></label><input data-campo="representatividade" value="${escapeHtml(amostra.representatividade)}" required><small class="error-message"></small></div><div class="field"><label>Categoria <span>*</span></label><select data-campo="categoria" required>${opcoesHtml(opcoes.categorias, amostra.categoria)}</select><small class="error-message"></small></div><div class="field"><label>Tratamento? <span>*</span></label><div class="radio-group" style="grid-template-columns:1fr 1fr"><label class="choice"><input type="radio" data-campo="tratamento" name="tratamento-${indice}" value="Sim" ${amostra.tratamento === 'Sim' ? 'checked' : ''}>Sim</label><label class="choice"><input type="radio" data-campo="tratamento" name="tratamento-${indice}" value="Não" ${amostra.tratamento !== 'Sim' ? 'checked' : ''}>Não</label></div><small class="error-message"></small></div><div class="field treatment-fields" data-treatment-fields ${amostra.tratamento === 'Sim' ? '' : 'hidden'}><div class="field-grid"><div class="field"><label>Produto <span>*</span></label><input data-campo="tratProduto" value="${escapeHtml(amostra.tratProduto)}"><small class="error-message"></small></div><div class="field"><label>Princípio ativo <span>*</span></label><input data-campo="tratPrincipioAtivo" value="${escapeHtml(amostra.tratPrincipioAtivo)}"><small class="error-message"></small></div><div class="field"><label>Dosagem <span>*</span></label><input data-campo="tratDosagem" value="${escapeHtml(amostra.tratDosagem)}"><small class="error-message"></small></div></div></div></div></div></article>`).join('');
    count.textContent = `${amostras.length} de 8`; addButton.disabled = amostras.length >= 8;
    sampleList.querySelectorAll('[data-campo]').forEach((campo) => campo.addEventListener('input', atualizarRegistro)); sampleList.querySelectorAll('[data-campo="tratamento"]').forEach((campo) => campo.addEventListener('change', atualizarTratamento)); sampleList.querySelectorAll('.remove-sample').forEach((botao) => botao.addEventListener('click', removerAmostra));
  }
  function atualizarRegistro(evento) { const campo = evento.target; const card = campo.closest('[data-amostra]'); const registro = amostras[Number(card.dataset.amostra)]; if (campo.type === 'radio' && !campo.checked) return; registro[campo.dataset.campo] = campo.value; }
  function atualizarTratamento(evento) { atualizarRegistro(evento); const card = evento.target.closest('[data-amostra]'); const bloco = card.querySelector('[data-treatment-fields]'); bloco.hidden = evento.target.value !== 'Sim'; }
  function removerAmostra(evento) { const indice = Number(evento.target.closest('[data-amostra]').dataset.amostra); amostras.splice(indice, 1); renderizarAmostras(); }
  addButton.addEventListener('click', () => { if (amostras.length < 8) { amostras.push(novoRegistro()); renderizarAmostras(); } });
  document.querySelectorAll('input[name="finalidade"]').forEach((radio) => radio.addEventListener('change', () => { const outros = document.querySelector('#finalidade-outros-wrap'); outros.hidden = radio.value !== 'Outros' || !radio.checked; document.querySelector('#bas-notice').hidden = radio.value !== 'Emissão de BAS' || !radio.checked; }));
  document.querySelector('#cpf-cnpj').addEventListener('input', (evento) => { evento.target.value = Validacoes.mascaraCpfCnpj(evento.target.value); });
  document.querySelector('#clear-button').addEventListener('click', () => { if (window.confirm('Deseja limpar todos os dados preenchidos?')) { form.reset(); amostras = [novoRegistro()]; renderizarAmostras(); document.querySelector('#finalidade-outros-wrap').hidden = true; document.querySelector('#bas-notice').hidden = true; limparStatus(); } });
  function limparStatus() { const status = document.querySelector('#form-status'); status.className = 'status'; status.textContent = ''; }
  function erro(elemento, mensagem) { Validacoes.adicionarErro(elemento, mensagem); return elemento; }
  function validar() {
    document.querySelectorAll('.invalid').forEach((elemento) => Validacoes.limparErro(elemento)); let primeiro = null; const finalidade = form.querySelector('input[name="finalidade"]:checked')?.value; const obrigatorios = [...form.querySelectorAll('[required]')].filter((campo) => !campo.closest('[hidden]'));
    obrigatorios.forEach((campo) => { if ((campo.type === 'checkbox' || campo.type === 'radio') ? !form.querySelector(`[name="${campo.name}"]:checked`) : !String(campo.value || '').trim()) { if (!primeiro) primeiro = erro(campo, 'Campo obrigatório.'); else erro(campo, 'Campo obrigatório.'); } });
    const documento = document.querySelector('#cpf-cnpj'); if (documento.value && !Validacoes.validarCpfCnpj(documento.value)) { erro(documento, 'Informe um CPF ou CNPJ válido.'); if (!primeiro) primeiro = documento; }
    sampleList.querySelectorAll('[data-amostra]').forEach((card, indice) => { const safra = card.querySelector('[data-campo="safra"]'); if (safra.value && !Validacoes.validarSafra(safra.value)) { erro(safra, 'Use o formato AAAA/AA, por exemplo 2023/24.'); if (!primeiro) primeiro = safra; } const trat = amostras[indice]; if (trat.tratamento === 'Sim') ['tratProduto','tratPrincipioAtivo','tratDosagem'].forEach((nome) => { const campo = card.querySelector(`[data-campo="${nome}"]`); if (!campo.value.trim()) { erro(campo, 'Obrigatório quando há tratamento.'); if (!primeiro) primeiro = campo; } }); });
    const selecionados = [...form.querySelectorAll('input[name="ensaios"]:checked')]; const ensaiosErro = document.querySelector('#ensaios-error'); ensaiosErro.textContent = ''; if (!selecionados.length) { ensaiosErro.textContent = 'Selecione pelo menos um ensaio.'; if (!primeiro) primeiro = form.querySelector('input[name="ensaios"]'); } if (finalidade === 'Emissão de BAS' && !selecionados.some((campo) => campo.hasAttribute('data-definitivo'))) { ensaiosErro.textContent = 'Para emissão de BAS, selecione pelo menos um ensaio definitivo.'; if (!primeiro) primeiro = form.querySelector('[data-definitivo]'); }
    if (!form.querySelector('#declaracao').checked) { document.querySelector('#declaracao-error').textContent = 'A declaração é obrigatória.'; if (!primeiro) primeiro = document.querySelector('#declaracao'); } else document.querySelector('#declaracao-error').textContent = '';
    if (primeiro) { primeiro.scrollIntoView({ behavior:'smooth', block:'center' }); primeiro.focus?.(); return false; } return true;
  }
  function coletar() { const dados = {}; new FormData(form).forEach((valor, chave) => { if (chave !== 'ensaios' && chave !== 'declaracao') dados[chave] = valor; }); dados.ensaios = {}; ensaios.forEach((nome) => { dados.ensaios[nome] = Boolean(form.querySelector(`input[name="ensaios"][value="${nome}"]`)?.checked); }); dados.declaracao = true; dados.amostras = amostras.map((amostra, indice) => ({ ...amostra, numero: indice + 1 })); return dados; }
  async function carregarOpcoes() { try { const resultado = await AppAuth.requisitarApi('listarOpcoes'); if (resultado.dados?.peneiras?.length && resultado.dados?.categorias?.length) opcoes = resultado.dados; } catch (erro) { const status = document.querySelector('#form-status'); status.textContent = `Não foi possível carregar as listas atualizadas. Foram carregadas opções locais: ${erro.message}`; status.className = 'status show error'; } renderizarAmostras(); }
  form.addEventListener('submit', async (evento) => { evento.preventDefault(); limparStatus(); if (!validar()) return; const botao = document.querySelector('#submit-button'); const status = document.querySelector('#form-status'); botao.disabled = true; status.textContent = 'Enviando solicitação…'; status.className = 'status show loading'; try { const resultado = await AppAuth.requisitarApi('salvarSolicitacao', { dados: coletar() }); status.textContent = `Solicitação enviada com sucesso. Número: ${resultado.dados.solicitacaoId}`; status.className = 'status show success'; window.scrollTo({ top: 0, behavior: 'smooth' }); } catch (erro) { status.textContent = erro.message; status.className = 'status show error'; botao.disabled = false; window.scrollTo({ top: 0, behavior: 'smooth' }); } });
  amostras.push(novoRegistro()); carregarOpcoes();
})();
